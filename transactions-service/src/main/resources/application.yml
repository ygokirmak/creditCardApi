spring:
  application:
    name: transactions-service
  rabbitmq:
    host: ${RABBIT_HOST}

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://user:password@localhost:8761}/eureka/

server:
  port: 8082